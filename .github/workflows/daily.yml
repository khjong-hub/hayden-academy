name: HAYDEN ACADEMY · Daily
on:
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          test -f index.html
          test -f daily/index.html
          test -f weekly/index.html
          test -f monthly/index.html
          test -f english/index.html
          echo "HAYDEN ACADEMY structure OK"
